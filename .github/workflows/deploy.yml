# ⚠️ DESABILITADO — o projeto migrou para deploy na Vercel.
# Este workflow de deploy na EC2 (SSH + PM2 + Nginx) foi desativado: o gatilho
# `on:` agora só permite disparo manual e o job não executa nada. O conteúdo
# original está preservado, comentado, ao final do arquivo, como referência.
# Antes de reativar, ver a task de correção do Terraform/provisionamento.

name: 🚀 Deploy Intergaláctico (desativado)

on:
  workflow_dispatch:

jobs:
  noop:
    name: 🛸 Deploy movido para a Vercel
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deploy na EC2 desativado — o projeto usa a Vercel."
          echo "Conteúdo original preservado (comentado) no arquivo do workflow."

# ── Workflow original (deploy na EC2 via SSH/PM2) — preservado para referência ──
#
#     steps:
#       - name: 🛸 Abduzindo o código...
#         uses: actions/checkout@v4
#
#       - name: 🚀 Deployando na EC2
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             set -euo pipefail
#             cd /opt/fatal-muuudel
#             git checkout -- .
#             git pull origin main
#             set -a; source /etc/fatal-muuudel.env; set +a
#             rm -rf node_modules .next
#             npm ci
#             npx prisma generate
#             : "${DATABASE_URL_MIGRATE:?DATABASE_URL_MIGRATE nao definida}"
#             DATABASE_URL="$DATABASE_URL_MIGRATE" npx prisma migrate deploy
#             npm run build
#             pm2 describe fatal-muuudel > /dev/null 2>&1 \
#               && pm2 reload fatal-muuudel --update-env \
#               || pm2 start npm --name fatal-muuudel --cwd /opt/fatal-muuudel --update-env -- start
#             pm2 save
